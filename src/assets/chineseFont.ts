// src/assets/chineseFont.ts - This should import a Chinese TrueType font
// The full Base64 encoding for a Chinese font would be several MB in size,
// so this is a placeholder. In a real implementation, you would include
// the actual Base64 string of a TrueType font file that supports Chinese characters.

// For production, you would use a CDN-hosted font instead of embedding it directly.
const chineseFontBase64 = "";

export default chineseFontBase64;